/**
 * DashboardPage — stub (full implementation Phase 4+).
 * Typography: pageTitle standard applied.
 */

const DashboardPage = () => (
  <div className="flex flex-col gap-6">
    <header>
      <h1 className="text-xl font-semibold text-textPrimary leading-tight">Dashboard</h1>
      <p className="text-sm text-textMuted">Summary metrics and recent activity will appear here.</p>
    </header>
  </div>
);

export default DashboardPage;
