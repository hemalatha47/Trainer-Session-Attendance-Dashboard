/**
 * SettingsPage — stub (full implementation Phase 9+).
 * Typography: pageTitle standard applied.
 */

const SettingsPage = () => (
  <div className="flex flex-col gap-6">
    <header>
      <h1 className="text-xl font-semibold text-textPrimary leading-tight">Settings</h1>
      <p className="text-sm text-textMuted">Configure profile, attendance threshold, and application preferences.</p>
    </header>
  </div>
);

export default SettingsPage;
