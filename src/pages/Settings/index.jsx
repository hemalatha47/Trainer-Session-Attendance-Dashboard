/**
 * SettingsPage — Module 2.3 compile-safety stub.
 * Profile form and threshold configuration arrive in Phase 9.
 */

const SettingsPage = () => (
  <div>
    <h2 className="text-lg font-semibold text-primary mb-2">Settings</h2>
    <p className="text-sm text-textMuted">
      Profile and attendance threshold configuration will appear here.
    </p>
  </div>
);

export default SettingsPage;
