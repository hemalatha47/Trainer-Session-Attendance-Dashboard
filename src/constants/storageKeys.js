/**
 * storageKeys.js
 * Single source of truth for localStorage key names — avoids key collisions.
 */

export const STORAGE_KEYS = {
  AUTH_SESSION: 'attendance_auth_session',
  APP_SETTINGS: 'attendance_app_settings',
  ATTENDANCE_DATA: 'attendance_mock_data',
};
